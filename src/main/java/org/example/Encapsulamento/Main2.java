package main.java.org.example.Encapsulamento;

import javax.swing.*;

public class Main2 {
    public static void main(String[] args) {
       /* ContaBancaria conta = new ContaBancaria(1234);
        System.out.println("Numero da conta: " + conta.getNumeroConta());
        System.out.println(conta.getSaldo());
        conta.depositar(10);
        System.out.println(conta.getSaldo());
        conta.sacar(5);
        System.out.println(conta.getSaldo());*/


      /* Pessoa p1 = new Pessoa();
      p1.nome1();
      p1.idade1();
      p1.cpf1();

       p1.exibirInformacoes();*/

       /* Produto produto = new Produto();
        produto.setNome(produto.getNome());
        produto.setPreco(produto.getPreco());
        produto.setQuantidadeEstoque(produto.getQuantidadeEstoque());
        produto.adicionarEstoque(produto.getQuantidadeEstoque());
        produto.vender(produto.getQuantidadeEstoque());
        produto.exibir();*/

       /* Aluno aluno = new Aluno("alan", 249030);
        aluno.getMatricula();
        aluno.getNota();
        aluno.getNome();
        aluno.setNota(7);

        aluno.exibirAluno();*/

       /* Retangulo retangulo = new Retangulo();
        retangulo.setAltura(Integer.parseInt(JOptionPane.showInputDialog("Altura do retangulo: ")));
        retangulo.setLargura(Integer.parseInt(JOptionPane.showInputDialog("Largura do retangulo: ")));
        retangulo.perimetro();
        retangulo.area();*/

        Livro livro = new Livro("Dragão", "Alan", 2012);

        livro.exibirLivro();









    }
}
